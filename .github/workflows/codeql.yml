# =============================================================================
# CodeQL - Análise de Segurança Avançada
# =============================================================================
# Análise estática de código para detectar vulnerabilidades de segurança
# Executa em: push, pull requests e semanalmente (segunda-feira às 00:00 UTC)
# =============================================================================

name: 🔒 CodeQL - Análise de Segurança

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    # Executa toda segunda-feira às 00:00 UTC
    - cron: '0 0 * * 1'

jobs:
  # ===========================================================================
  # JOB: Análise CodeQL
  # ===========================================================================
  analise-codeql:
    name: 🔍 Analisar Código
    runs-on: ubuntu-latest
    timeout-minutes: 15

    # Evita falhas em PRs de forks (token não tem permissão para enviar security-events).
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository

    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ['javascript-typescript']

    steps:
      - name: 📥 Baixar código fonte
        uses: actions/checkout@v4

      - name: 🔧 Inicializar CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: +security-and-quality

      - name: 🏗️ Build automático
        uses: github/codeql-action/autobuild@v3

      - name: 🔍 Executar Análise CodeQL
        uses: github/codeql-action/analyze@v3
        with:
          category: '/language:${{ matrix.language }}'

      - name: 📊 Resumo da Análise
        if: always()
        run: |
          echo "## 🔒 Análise de Segurança CodeQL" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Informações da Análise:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Valor |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🗂️ Linguagem | JavaScript/TypeScript |" >> $GITHUB_STEP_SUMMARY
          echo "| 🌿 Branch | \`${{ github.ref_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 📝 Commit | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| ⏰ Data | $(date -u '+%d/%m/%Y %H:%M UTC') |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ Status:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "A análise CodeQL foi executada com sucesso." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📚 Próximos Passos:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Verifique a aba **Security** do repositório para ver alertas" >> $GITHUB_STEP_SUMMARY
          echo "- Vulnerabilidades encontradas serão listadas em **Code scanning alerts**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "💡 **Nota:** Se o Code Scanning não estiver habilitado, este workflow não enviará resultados." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Para habilitar: **Settings > Code security and analysis > Code scanning**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📚 [Documentação CodeQL](https://docs.github.com/code-security/code-scanning)" >> $GITHUB_STEP_SUMMARY
