/**
 * Tipos para badges de status do projeto
 */

export interface StatusBadgeTheme {
  bgGradient: [string, string];
  cardBg: string;
  primaryColor: string;
  secondaryColor: string;
  textColor: string;
  accentGradient: [string, string];
  borderColor: string;
}
