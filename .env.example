# URL base do site (opcional para dev)
# Use quando você quiser forçar o domínio no ambiente local.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# URL canônica (OBRIGATÓRIA em produção na Vercel)
# Usada para gerar URLs absolutas (ex: snippets de badges/banners para README).
NEXT_PUBLIC_CANONICAL_URL=https://seu-dominio

# GitHub Token (opcional mas recomendado para higher rate limits)
# Crie um token em https://github.com/settings/tokens com acesso a "public_repo"
# Usado para consultas nas APIs de GitHub Stats e GitHub Langs
GITHUB_TOKEN=ghp_seu_token_aqui

